"use client";import{useState}from"react";import type{HouseholdSummary}from"@/lib/data";
export function HouseholdSettings({initialHousehold}:{initialHousehold:HouseholdSummary}){const[draft,setDraft]=useState(initialHousehold);const[message,setMessage]=useState("");async function save(){const response=await fetch("/api/household",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(draft)});const body=await response.json()as{message?:string};setMessage(body.message??"");}return <details className="settings-disclosure card"><summary><span>⌂</span><span><strong>Household settings</strong><small>Name, timezone, and minimum cash buffer</small></span></summary><div className="household-settings-form"><div className="field"><label>Name</label><input value={draft.name} onChange={(event)=>setDraft({...draft,name:event.target.value})}/></div><div className="field"><label>Timezone</label><input value={draft.timezone} onChange={(event)=>setDraft({...draft,timezone:event.target.value})}/></div><div className="field"><label>Minimum cash buffer</label><input type="number" min="0" step="0.01" value={draft.minimumCashBufferCents/100} onChange={(event)=>setDraft({...draft,minimumCashBufferCents:Math.round(Number(event.target.value)*100)})}/></div><button className="primary-button" onClick={()=>void save()}>Save household</button><p className="form-message">{message}</p></div></details>}
